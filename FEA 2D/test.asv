clc; clear all

refined=2;
h=0.01;
v=0.25;
E=200e9;
L=10;

pitch=L/refined;

pts=[[0 0];[pitch 0];[pitch pitch];[0 pitch]];
ke=k_eMatrix(pts,h,v,E);  
ke_aux=zeros(length(ke)+1);
ke_
Allnames=function_names(refined);

k=zeros((refined+1)^2);

for i=1:length(Allnames)
    label=Allnames(i,:)
    
end